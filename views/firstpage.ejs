<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Jersey Store</title>
  <link rel="stylesheet" href="/css/navandfoot.css">
  <link rel="stylesheet" href="/css/firstpage.css">
</head>

<body>
  <nav>
    <div class="logo">
      <a href="/">Home</a>
    </div>
    <div class="search">
      <input type="text" id="searchInput" class="nav-search-input" placeholder="Search">
      <a href="#" class="nav-search-icon">
        <img src="/assets/search_icon.png" alt="search-icon">
      </a>
    </div>
    <% if (user) { %>
      <a href="/logout" class="account">Logout (<%= user.username %>)</a>
    <% } else { %>
      <a href="/signup" class="account">Login / Sign-in</a>
    <% } %>
    <a href="/cart" class="cart">
      <img src="/assets/cart_icon.png" alt="cart-icon">
      Cart
    </a>
  </nav>

  <div class="welcome">
    <% if (user) { %>
      Welcome, <%= user.username %>!
    <% } else { %>
      Welcome, Guest!
    <% } %>
  </div>

  <div class="cards" id="productContainer">
    <!-- Products will be injected here dynamically -->
  </div>

  <footer>
    <textarea name="feedback" id="101" placeholder="Please enter your feedback" rows="5"></textarea>
    <input type="submit" value="Submit" id="foot-submit">
    <div>Contact us:</div>
  </footer>

  <!-- Include JS Files -->
  <script src="/js/products.js"></script>
  <script src="/js/home.js"></script>
</body>

</html>
